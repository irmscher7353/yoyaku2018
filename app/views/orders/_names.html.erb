<%= button_tag('Ｘ', id: 'close-names-panel', type: :button, remote: true,
    onClick: "orders.close_names_panel(this);", ) %>
<% names.each do |name| %>
<%= button_tag(name, type: :button, remote: true,
    onClick: "orders.set_last_name(this);", ) %>
<% end %>
