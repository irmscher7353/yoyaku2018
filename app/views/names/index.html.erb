<p id="notice"><%= notice %></p>

<h1>Names</h1>

<table>
  <thead>
    <tr>
      <th><%= t 'names.value' %></th>
      <th><%= t 'names.is_shamei' %></th>
      <th><%= t 'names.is_sitenmei' %></th>
      <th><%= t 'names.is_sei' %></th>
      <th><%= t 'names.is_mei' %></th>
      <th><%= t 'names.is_title' %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @names.each do |name| %>
      <tr>
        <td><%= name.value %></td>
        <td><%= name.is_shamei %></td>
        <td><%= name.is_sitenmei %></td>
        <td><%= name.is_sei %></td>
        <td><%= name.is_mei %></td>
        <td><%= name.is_title %></td>
        <td><%= link_to t('Show'), name,
                class: 'link-show-inline' %></td>
        <td><%= link_to t('Edit'), edit_name_path(name),
                class: 'link-edit-inline' %></td>
        <td><%= link_to t('Destroy'), name,
                class: 'button-destroy-inline',
                method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<div id="pager" >
<%= paginate @names %>
</div>

<br>

<%= link_to t('New'), new_name_path, class: 'link-new' %>
