<%= form_with(model: preference, local: true) do |form| %>
  <% if preference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(preference.errors.count, "error") %> prohibited this preference from being saved:</h2>

      <ul>
      <% preference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :preference_name, size: 42 %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_area :value, id: :preference_value, size: "40x10" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
