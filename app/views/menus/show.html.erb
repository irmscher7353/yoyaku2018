<p id="notice"><%= notice %></p>

<p>
  <strong><%= t 'menus.name' %>:</strong>
  <%= @menu.name %>
</p>

<%= link_to t('Edit'), edit_menu_path(@menu), class: 'link-edit' %> |
<%= link_to t('Back'), menus_path, class: 'link-back' %>
